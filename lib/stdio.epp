class stdio:
    @staticmethod
    def print(*args):
        temp = "".join(str(arg) for arg in args)
        print(temp.replace(r'\n', '\n'))

    @staticmethod
    def get(prompt_text):
        return input(prompt_text)
